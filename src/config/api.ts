// API Configuration - Use VITE_API_URL with sensible default
const API_BASE_URL = 'https://hefty.cc';

// Construct lead ingestion endpoint
export const LEAD_INGEST_URL = `${API_BASE_URL}/api/leads/ingest`;

export { API_BASE_URL };